import { DonorBadgePipe } from './donor-badge-pipe';

describe('DonorBadgePipe', () => {
  it('create an instance', () => {
    const pipe = new DonorBadgePipe();
    expect(pipe).toBeTruthy();
  });
});
